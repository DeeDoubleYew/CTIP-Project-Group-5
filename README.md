README

For installing python dependancies, use pip install -r requirements.txt
For installing node dependancies npm install
To Launch dev server for app 1. cd spam-app(or open terminal here) 2. npm start
